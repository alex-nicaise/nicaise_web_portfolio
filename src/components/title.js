import React from 'react';

const Title = props => (
    <div className="title-outer">
        <div className="title-h2">
            <h2>{props.name}</h2>
        </div>
    </div>
);

export default Title;